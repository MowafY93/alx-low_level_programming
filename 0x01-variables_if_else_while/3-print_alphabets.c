#include<stdio.h>
/**
 *  * main - Entry point
 *   *
 *    * Return: Always 0 (Success)
 *     **/
int main(void)
{
	char ch = 'a';
	char CH = 'A'
		while (ch <= 'z')
		{
			putchr(ch);
			ch++;
		}
	while (CH <= 'Z')
	{
		putchar(CH);
		CH++;
		return (0);
	}
